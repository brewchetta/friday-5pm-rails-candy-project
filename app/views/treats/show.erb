<h1><%= @treat.name %></h1>

<p><b>Rating: <%= @treat.rating %></b></p>

<p><%= @treat.ingredients %></p>

<%= image_tag @treat.picture_url, class: "treat-img" %>

<%= button_to "Delete", treat_path(@treat), method: :delete, data: { confirm: "Are you sure? You'll lose #{@treat.name} forever!" } %>
